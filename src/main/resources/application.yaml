server:
  port: 8080  # Change this to another port (e.g., 8081) if 8080 is busy

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/access_manager
    username: root
    password: Prethi@12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # creates/updates table automatically
    show-sql: true       # shows SQL queries in console
    properties:
      hibernate:
        format_sql: true

  # 📧 EMAIL CONFIGURATION
  mail:
    host: smtp.gmail.com
    port: 587
    username: prethisk1210@gmail.com
    password: luoctdypvhaklgaw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
